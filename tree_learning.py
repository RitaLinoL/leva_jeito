from BTrees.OOBTree import OOBTree
import os
import hashlib

try:
    os.makedirs("./temp")
except OSError:
	print("error")

guarda = OOBTree()
print(type(t))



if o:
	

if os.path.isdir(directory):
	directory = OOBTree()
	guarda.update({name: directory})
	guarda.update({name: directory})

for i in t.keys():
	print(i)